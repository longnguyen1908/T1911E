#include <stdio.h>
float main(){
	float a,b;
	printf("nhap vao 2 so a ,b \n");
	scanf("%f%f",&a,&b);
	if (a>=b){
			float thuong = a / b;
			printf("ket qua cua phep tinh a/b=%f",thuong);
		}else{
			if (a<b);
			float tich = a * b;
			printf ( "ket qua cua a*b =%f",tich);
		}
	}
